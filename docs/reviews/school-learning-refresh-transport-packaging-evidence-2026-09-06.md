# School Learning Refresh Transport and Packaging Evidence — 2026-09-06

This ordinary dated record is non-canonical checkpoint evidence. Authority is
owned by the current owner request and interpreted under `AGENTS.md`;
architecture belongs to `docs/architecture/school-learning.md`. Engineering
Sessions, Engineering Collaboration, Engineering Lifecycle, and Engineering
Review retain their canonical contracts.

## Accepted checkpoint brief

- **Why:** Remove folder navigation, evidence selection/copying, prompt
  reconstruction, candidate-file creation, and Markdown-corrupted JSON friction
  from the proven manual course-refresh loop.
- **Risk tier:** Tier 3: explicit external local files enter a portable
  AI-consumable package, crossing a privacy/export boundary.
- **Exact scope:** Additive one-step `prepare-refresh`, explicit transient
  evidence and exact notes, deterministic packaging, optional CLI opening, and
  synthetic native verification. Exactly `tools/school_learning/core.py`,
  `tools/school_learning/cli.py`, `tools/school_learning/__init__.py`,
  `tests/test_school_learning.py`, `docs/architecture/school-learning.md`, and
  this evidence record are writable repository paths.
- **Exclusions:** No seventh path, renderer/schema/apply-semantics changes,
  durable evidence promotion, discovery/crawling, integrations, learner changes,
  dependencies/configuration changes, live School data, staging, commits,
  branch/ref mutation, publication, deployment, or unrelated external writes.
- **Authority established:** The attached current owner instruction explicitly
  accepts the design and authorizes implementation and repository-native
  synthetic verification in the six paths. Candidate acceptance is not yet
  established. Local commit, publication, and deployment are not authorized.
- **Protected boundaries:** No secrets, credentials, protected refs, live/private
  School data, network actions, or changes to generated ownership/state/mission.
  Preserve unrelated user changes. Only task-owned synthetic external inputs.
- **Observable result:** One ready directory containing strict context/contract,
  separate refresh context, selected attachments, protocol, and an initially
  absent owner-return slot; unchanged manual reviewed apply remains mandatory.
- **Verification:** Native WSL/Python; focused School Learning and final full
  Python discovery suites; adversarial export/publication/return/opener tests;
  Atlas validate/missing/sync and complete Git diff/path checks. Fresh independent
  adversarial Tier-3 review follows this implementation; it is not claimed here.
- **Stop conditions:** Owner-listed scope, architecture, privacy, verification,
  and protected-operation boundaries plus Engineering Lifecycle anti-loop stops.
- **Next decision boundary:** Fresh independent Tier-3 review of the exact final
  uncommitted candidate, then owner acceptance; any later publication requires
  separate explicit authority.

## Repository and native preflight

Observed local `main` and locally recorded `origin/main` both at
`7e40290450e95ce15bdb22d77cf61fd4349ada88`, exactly matching the supplied handoff.
Ahead/behind was `0 0`; staged, unstaged, and untracked inspections were clean.
No fetch or network refresh occurred. Atlas bootstrap was valid, healthy within
scope, and synchronized; canonical state/mission were intentionally idle and
do not grant authority. The current owner instruction supplies this checkpoint.
Atlas missing reported no missing definitions; sync had zero warnings/errors.

Native environment: resolved selected repository root (absolute owner path
omitted from public evidence), Ubuntu-22.04 WSL, Microsoft WSL2 Linux kernel
version components 6 / 6 / 87 / 2, Python `3.10.12`, Git `2.34.1`.
Authorized target permissions were writable. Task preflight temporary directory:
`/var/tmp/school-refresh-preflight.CJQDxH`; School fixtures use isolated
`TemporaryDirectory(dir="/var/tmp")` roots and deterministic synthetic content.
No live Fall 2026 data root was accessed. Shell network actions remained unused;
no dependency or configuration remediation occurred. The desktop sandbox
execution helper failed before product execution; approved elevated WSL commands
provided the accepted native environment. This was an environment failure, not
a product result.

Preflight focused command:

    PYTHONPATH=tools PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_school_learning.py'

Baseline: 123 tests passed. All existing ordinary course-handoff tests remain
in the expanded suite. The prior blanket subprocess-import assertion conflicted
with the explicitly accepted CLI opener and was narrowed to keep subprocess
forbidden in core/render while directly testing CLI argument arrays, exact path
conversion, opt-in behavior, and failure isolation.

## Implementation and risk analysis

Shared course-handoff content construction avoids a second context or update
contract architecture. Refresh packages use separate deterministic directories;
existing packages are validated without replacement, preserving owner returns.
The candidate schema, preview, digest, stale checks, and apply implementation
are unchanged. Exported evidence identity depends on full content SHA-256;
basename metadata changes the package identity but not evidence identity.
Exact UTF-8 note bytes survive JSON round-trip, including whitespace and CRLF.

Explicit no-follow descriptor traversal, regular-file checks, finite count/byte
budgets, and pre-publication source rechecks constrain the export boundary.
Only explicitly selected sources are read. Transient bytes stay package-local;
no material manifest, source observation, descriptor, or academic state is
inferred. Owner notes and opaque evidence remain untrusted situational context
under stable AI instructions. Prompt instructions guide an external consumer;
they do not mechanically guarantee external AI compliance or owner approval.

The existing atomic publication/recovery machinery handles new directories;
reruns never replace the owner-return slot. Negative tests inject failures before
and after rename and incomplete cleanup. An incomplete-recovery result remains
an error, not a claim of successful rollback. No automatic retention policy is
introduced. Host opening is CLI-only, with mocked process tests; no real GUI
opening or external AI upload was performed.

## Verification commands and result anchors

Final checks must follow the last repository mutation. The exact command set is:

    PYTHONPATH=tools PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_school_learning.py'
    PYTHONPATH=tools PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_*.py'
    PYTHONDONTWRITEBYTECODE=1 ./atlas validate
    PYTHONDONTWRITEBYTECODE=1 ./atlas missing
    PYTHONDONTWRITEBYTECODE=1 ./atlas sync
    git diff --check
    git diff --stat
    git diff --name-status
    git diff --cached --name-status
    git status --short --branch --untracked-files=all

The expanded focused suite passed all 143 tests. The first full run executed
558 tests and reported three public-surface assertion failures in this evidence
record: an absolute checkout path also contained the historical repository name,
and the dotted kernel release matched the IP-literal gate. The owner path was
omitted and kernel components were recorded separately in this file; production
code and validation rules were not changed. One guarded historical B2a
integration test was skipped because it requires separate explicit enablement.
This ordinary in-scope documentation correction requires renewed final checks.

The full suite's isolated fixtures are separate from Atlas's explicitly named
current-repository metadata/state/generated-synchronization smoke checks. Final
execution outcomes and complete diff/path inspection are recorded in the task
completion report after this record's final write; no unexecuted command is
represented here as passing.

The candidate remains uncommitted on the base above. Independent review, owner
candidate acceptance, and publication/deployment have not occurred. Remaining
limits: bounded POSIX/WSL external reads, mocked rather than real GUI behavior,
no live School-data or external AI-surface validation, and no automatic retention.


## Launch-time opener correction

The owner reported that the fresh independent review could not execute because
its local process runtime failed before execution. This is a review-blocked
environment result, not a valid Tier-3 verdict. The reviewer supplied a
preliminary MATERIAL concern from the captured diff: conversion validation did
not revalidate the exact completed non-symlink package before Explorer launch.
Inspection confirmed the concern in the preserved implementation.

Re-established local main/HEAD/tracking, Atlas, empty index, and the exact six
changed paths. The preserved six-file fingerprint matched
`a80bdd640326ec7250a33c6b586203dc76509207692b97a40181dc3ac4430e4a`.
Native Python and task-owned writable temporary fixtures were rechecked; the
sandbox helper again failed before execution and approved WSL execution supplied
the native environment. No live School data or network action was involved.

The correction is CLI-only: validate the completed content-addressed manifest,
fixed course location, no-follow paths, generated bytes, attachment identities,
and package directory device/inode before conversion; repeat validation
immediately before host launch and require the same directory identity. Symlink
swaps, missing/tampered packages, and byte-identical directory replacements warn
and suppress Explorer. Package generation, reviewed candidate semantics, and
owner-return handling are unchanged. The opener uses the same argument arrays
and timeouts. No actual host launch was used for verification.

New adversarial synthetic cases replace the package after preparation and after
path conversion with a symlink, byte-identical copy, absence, or tampered content.
They require no Explorer invocation, visible warning, and preserved CLI success
for a completed construction. The post-correction focused run passed all 145
School Learning tests. Final focused and broad verification are rerun after
this evidence update so their results apply to the last repository mutation. Exact post-correction focused/full/Atlas/Git
commands are the verification command set above; final outcomes follow the last
repository mutation and are recorded in the completion report. This record does
not claim a valid independent review or owner acceptance. The next gate is fresh
independent Tier-3 review of the corrected exact candidate.

Residual boundary: Explorer consumes a pathname rather than the held directory
descriptor. Immediate pre-launch revalidation narrows the observed swap window
but cannot make Explorer's later filesystem lookup atomic against a concurrent
writer. No claim of an atomic cross-host filesystem handoff is made.


## Valid independent review and material identity collision correction

The owner supplied the completed fresh independent Tier-3 review result:
**CORRECTION REQUIRED**, with no BLOCKING or MINOR findings and one MATERIAL
finding. Unlike the earlier environment-blocked attempt, this review executed
successfully. The reviewed six-path uncommitted candidate was
`7991ed75214262b29e4bb6c9f7e45a7e2f66c35f9c8fe8b3e60ecf463788eebb`.
Startup re-established that exact fingerprint, unchanged main/HEAD/tracking,
empty index, exactly six changed paths, valid/synchronized Atlas state, Python
3.10.12, and a task-owned writable synthetic temporary root. The sandbox helper
failed before execution; approved native WSL execution remained available.

The MATERIAL finding was reproduced using disposable synthetic course data:
the accepted durable component grammar permits `evidence-<full-sha256>`, so a
computed transient identity could also occur as a durable material ID in the
refresh manifest. Strict reviewed-update validation legitimately accepted that
ID as a durable reference. Changing the candidate validator or globally reserving
the prefix would incorrectly alter existing durable compatibility.

The correction is confined to `prepare_refresh`: collect every existing durable
material ID for the course, then reject a computed transient ID that collides,
with a clear error before any staging directory or publication. Unselected
durable materials participate in this guard. The existing final course-context
recheck continues to reject concurrent durable-state changes before publication.
No grammar, parser, reviewed-update/apply implementation, opener, or package
identity formula was changed.

Two regression tests establish that the evidence-shaped durable ID remains
accepted, selected and unselected collisions fail before staging/publication,
the complete course tree and an existing owner return remain unchanged,
non-colliding evidence still succeeds, and the durable ID remains usable through
strict preview and digest-confirmed apply. Wrong confirmation and stale-base
rejection remain covered. The post-correction focused suite passed 147 tests.

Corrected implementation fingerprint (the five implementation/test/architecture
paths, excluding this evidence file to avoid a self-referential digest):
`d15c8d35a38975cdefbcc83b05d15f429aaea62c4dba13a27e8ad7e36faf3de6`.
It is SHA-256 of compact sorted-key JSON mapping each of those repository-relative
paths to its full file SHA-256. The final six-path fingerprint including this
record is reported in the completion report after this final write. No commit
identity is invented.

Final focused/full native suites, Atlas validate/missing/sync, complete six-path
diff inspection, whitespace checks, and staged/unstaged/untracked checks use the
exact command set above and run after the last mutation; final execution results
are recorded in the completion report. Only synthetic School data is used.
The next gate is fresh independent Tier-3 review of the corrected exact candidate.
The prior review is not a verdict on this correction. Owner acceptance, staging,
commit, publication, deployment, and live-data access are not claimed.
