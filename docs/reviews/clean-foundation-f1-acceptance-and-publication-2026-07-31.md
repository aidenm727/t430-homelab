# Clean Foundation F1 Acceptance and Publication — July 31, 2026

## Record Status

- Date: 2026-07-31
- Evidence class: Dated, non-canonical acceptance and publication evidence
- Decision authority: Owner
- Checkpoint: Clean Foundation F1 — Canonical State, Authority, and Orientation
- Candidate base: `0ffa8f5fe24d32d6bf32f1d866a882a3fea3176d`
- Final independent disposition: `A. ACCEPT — implementation-complete and suitable for owner acceptance`
- Owner acceptance: Explicit on 2026-07-31
- Publication authority: Separately explicit and bounded on 2026-07-31
- Record boundary: Acceptance and publication authority; Git history owns immutable publication identities

This record preserves the accepted F1 outcome, the evidence chain supporting
it, and the exact boundary of the separately authorized publication operation.
Canonical active state remains in `docs/current-state.json`, its compact human
companion remains in `docs/current-mission.md`, and Git history establishes
which commits were actually published.

## Purpose and Boundary

Clean Foundation F1 establishes the shared repository foundation needed for
truthful engineering-session orientation:

- strict typed canonical active state;
- explicit separation of phase, selected work, repository health, task
  authority, implementation authority, and publication authority;
- a compact Current Mission companion;
- `AGENTS.md` as the primary repository-local authority-interpretation
  contract;
- one shared Atlas readiness projection;
- exact generated-context active-state synchronization; and
- a concise Aiden Platform repository entry path.

The accepted boundary preserves School Learning and task-scoped context
compilation behavior. It does not implement F2 metadata or lifecycle work, F3
control-surface work, another capability, deployment, dependency or runtime
change, CI, infrastructure change, or Project synchronization.

## Evidence and Review Chain

The accepted outcome is supported by the complete live candidate, canonical
repository content and history, current Atlas output, and this external review
chain:

1. The implementation report recorded the original 37-path candidate at base
   `0ffa8f5fe24d32d6bf32f1d866a882a3fea3176d` and requested independent review.
2. The first independent review returned `C. CORRECTION REQUIRED` with three
   blocking contract defects and one non-blocking guidance issue.
3. The bounded correction resolved exact generated-context synchronization,
   removed an unsupported local-verification claim from canonical state,
   removed the residual retired authority terminology, and prioritized the
   pending owner decision without granting authority.
4. Native post-correction verification ran in an environment with writable
   `/var/tmp` and established the corrected candidate without repository
   mutation.
5. The final independent re-review inspected the complete candidate and
   returned exactly `A. ACCEPT — implementation-complete and suitable for owner
   acceptance`, with no blocking or non-blocking implementation findings.
6. On 2026-07-31, the owner explicitly accepted the corrected 37-path F1
   candidate as implementation-complete.
7. The owner separately authorized only the bounded two-commit acceptance
   recording and publication operation described below.

The original implementation report describes the pre-correction candidate.
Its superseded statements and test counts are resolved by the correction,
native post-correction verification, and final independent re-review. The
evidence chain is therefore consistent when read in sequence.

## Accepted Outcome

The owner accepts the final independent findings that:

- all prior review defects are resolved;
- no blocking or non-blocking implementation findings remain;
- the complete accepted candidate contains exactly 37 paths;
- canonical state is strict, typed, fail-closed, and authority-safe;
- Current Mission is a compact canonical companion;
- Atlas reports repository health, work selection, and authority as distinct
  dimensions through one shared projection;
- generated context is checked against the exact canonical active-state
  projection;
- School Learning and task-scoped context behavior are preserved; and
- no protected path, ref, configuration, dependency, or external system was
  changed during implementation, correction, native verification, or review.

This acceptance establishes that the local candidate is
implementation-complete. It does not make repository state or Atlas a source
of current task, implementation, or publication authority.

## Native Verification and Atlas Evidence

The exact post-correction native result was:

- focused active-state tests: 42 tests, OK;
- focused readiness tests: 18 tests, OK;
- School Learning tests: 74 tests, OK;
- context tests: 228 tests, OK, with one existing skip;
- complete native suite: **405 tests, OK, with one existing skip**; and
- all required Atlas commands exited successfully.

Before publication, Atlas reported:

- Repository Health: Healthy within declared scope;
- Validation: Valid, with zero errors and warnings;
- Synchronization: Synchronized, with zero errors and warnings;
- Work Selection: selected;
- Selected Checkpoint: Clean Foundation F1;
- Blockers and unknowns: none; and
- task, implementation, and publication authority: external and not
  established by repository state or Atlas.

## Owner Acceptance and Publication Authority

Owner acceptance and publication authority are separate decisions.

The owner first accepted the corrected 37-path local F1 candidate as
implementation-complete on 2026-07-31. That acceptance alone authorized no
repository modification, lifecycle update, staging, commit, push, publication,
deployment, Project synchronization, F2, F3, or external write.

The owner subsequently and separately authorized:

- bounded repository changes needed to record the accepted outcome and final
  published/intentional-idle state;
- regeneration of repository-owned generated context;
- non-destructive local and remote verification;
- staging only the exact authorized publication paths;
- creation of the minimum coherent commits required by immutable evidence;
- non-force pushes from local `main` to `origin/main`; and
- final local, tracking, remote, generated-artifact, and Atlas verification.

No pull request, merge, rebase, force push, tag, release, deployment, Project
inspection or synchronization, F2, F3, dependency change, configuration
change, infrastructure change, or unrelated external write is authorized.

## Immutable-Evidence Publication Structure

The canonical active-state evidence contract requires a known immutable commit
identity. A file cannot truthfully contain the identity of the same commit that
creates it. Publication therefore uses exactly two coherent commits:

1. publish the independently accepted 37-path implementation candidate and
   this dated acceptance/publication evidence record while canonical state
   truthfully remains at publication finalization; then
2. use the exact first commit identity to finalize canonical F1 `published`
   and `intentional_idle` state in `docs/current-state.json`,
   `docs/current-mission.md`, and generated `docs/aiden-context.md`.

The first publication commit identity is supplied by Git history after commit
creation. It is deliberately not predicted or guessed in this file. The final
state-publication commit identity is likewise supplied by Git history and
remote verification.

## Protected-Path Preservation

The accepted candidate changes only the independently reviewed 37 F1 paths.
All tracked paths outside that set remain byte-identical to the base. In
particular:

- School Learning implementation, architecture, launcher, tests, and local
  personal-data boundary are unchanged;
- task-scoped context implementation, schemas, policies, fixtures, selection,
  materialization, validation, and tests are unchanged;
- dependencies, configuration, CI, infrastructure sources, services, images,
  archives, roadmaps, and Engineering Opportunity lifecycle are unchanged; and
- `docs/infrastructure-snapshot.md` remains byte-identical with SHA-256
  `966a6b181ab229b4e708162cc5bdbb7edb874dafa9b3af3aac2aa14c7b1367b3`.

Only the three canonical state companion paths may change after the first
publication commit.

## Excluded Actions

No pull request, merge, rebase, force push, tag, release, deployment, Project
inspection or synchronization, F2, F3, dependency, runtime, CI,
configuration, secret, credential, infrastructure, School Learning,
task-context, or unrelated work is part of this checkpoint.

At record creation, none of those actions had occurred. Completion of the two
authorized non-force pushes and the exact published commit identities must be
established from Git history and fresh remote verification rather than claimed
prospectively by this record.

## Final Boundary

After the two authorized commits are published and verified, F1 is represented
as published and work selection becomes intentional idle with no selected
checkpoint. Selecting a future engineering checkpoint remains an explicit
owner decision. ChatGPT Project synchronization remains a separate
owner-controlled action and is not performed by this publication operation.
