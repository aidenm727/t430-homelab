date: 2026-06-23
title: Improve change capture workflow
change_type: documentation
status: verified

intent:
  - Improve structured change records after using the workflow during the virtualization-host storage change

summary:
  - Added intent extraction to YAML change records
  - Updated CLI usage text to include the intent command
  - Added the intent command to the change-capture helper

verification:
  - Verified the intent command appended entries under the Intent section

documentation_outputs:
  - Updated YAML generation to include the Intent section

source_session: docs/change-session.md
